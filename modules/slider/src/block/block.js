(
	function() {

		const { registerBlockType } = wp.blocks;
		const {
			createElement,
			Fragment,
			Component,
			RawHTML
		} = wp.element;
		const {
			ExternalLink,
			Path,
			Rect,
			SVG
		} = wp.components;
		const { __ } = wp.i18n;
		const {
			BlockIcon,
			InnerBlocks,
			InspectorControls,
			PanelColorSettings,
			ContrastChecker,
		} = wp.blockEditor;

		//=require ./slide/block.js
		//=require ./slider/block.js

	}
)();

