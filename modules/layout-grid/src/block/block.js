(
	function() {

		const { registerBlockType } = wp.blocks;
		const {
			createElement,
			Fragment,
			Component,
		} = wp.element;
		const {
			select
		} = wp.data;
		const {
			ExternalLink,
			Placeholder,
			TextControl,
			ButtonGroup,
			Button,
			Tooltip,
			Path,
			SVG,
			PanelBody,
			RadioControl,
			RangeControl,
			ToggleControl,
			SelectControl,
		} = wp.components;
		const { __ } = wp.i18n;
		const {
			BlockIcon,
			InnerBlocks,
			InspectorControls,
			PanelColorSettings,
			ContrastChecker,
		} = wp.blockEditor;

		//=require ./icons.js

		//=require ./column/block.js

		//=require ./grid/block.js

	}
)();
